// 変数の宣言
  let weight;                         // 体重（kg）
  let height;                         // 身長（m）
  let bmi;                            // bmi
// 変数の宣言・値の代入
  weight = 68;                     // 体重（kg）
  height = 1.7;                    // 身長（m）
  bmi = weight / (height * height);// bmi計算式
// コンソールへの出力
  console.log(bmi);